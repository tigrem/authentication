datasource db {
  provider = "postgresql"
  // THIS LINE MUST BE REMOVED: url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
// ... your model definition
}
