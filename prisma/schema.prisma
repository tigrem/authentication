datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // <-- THIS LINE IS REQUIRED FOR PRISMA 4.16.2
}

generator client {
  provider = "prisma-client-js"
}

// ... your models ...
